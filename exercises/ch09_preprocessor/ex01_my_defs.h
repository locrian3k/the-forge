/*
  ex01_my_defs.h
  Exercise 9.1 — Area header file
  Define path constants, an area name, and a utility macro.
*/

// TODO: Define MY_PATH as a base path string
//   Example: #define MY_PATH "/players/me/myarea/"

// TODO: Define MY_ROOMS as MY_PATH + "rooms/"
//   Wrap the concatenation in parentheses to prevent precedence bugs.
//   Example: #define MY_ROOMS (MY_PATH + "rooms/")

// TODO: Define MY_MOBS as MY_PATH + "mobs/"
//   Same pattern as MY_ROOMS.

// TODO: Define AREA_NAME as a string constant
//   Example: #define AREA_NAME "The Training Grounds"

// TODO: Define a MAX(a, b) utility macro using the ternary operator.
//   Parentheses around (a) and (b) prevent precedence bugs when
//   the macro arguments are expressions like (x + 1).
//   Pattern: #define MAX(a, b) ((a) > (b) ? (a) : (b))
